<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="create-table-status-orcamento" author="elielson.silva">
        <preConditions onFail="MARK_RAN"
                       onFailMessage="Tabela status_orcamento já existe, pulando criação.">
            <not>
                <tableExists tableName="status_orcamento"/>
            </not>
        </preConditions>

        <createTable tableName="status_orcamento">
            <column name="id" type="numeric">
                <constraints primaryKey="true"/>
            </column>
            <column name="descricao" type="VARCHAR(100)">
                <constraints unique="true" nullable="false"/>
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>
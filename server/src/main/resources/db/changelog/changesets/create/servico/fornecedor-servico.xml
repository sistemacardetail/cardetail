<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="create-table-fornecedor-servico" author="elielson.silva">
        <preConditions onFail="MARK_RAN"
                       onFailMessage="Tabela fornecedor_servico já existe, pulando criação.">
            <not>
                <tableExists tableName="fornecedor_servico"/>
            </not>
        </preConditions>

        <createTable tableName="fornecedor_servico">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true"/>
            </column>
            <column name="nome" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="id_telefone" type="UUID">
                <constraints foreignKeyName="fk_fornecedor_telefone" references="telefone(id)"/>
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>